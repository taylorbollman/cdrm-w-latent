# Fuzzy-recall milestone change inventory

This inventory covers the implementation following the approved [150M fuzzy-recall plan](../../cdrm-150m-fuzzy-recall-plan.md). All six T256 calibration endpoints and the audited online aggregate are complete; reviewed numerical, selected-checkpoint and retention outcomes are tracked in [results.md](results.md). This review endpoint precedes the final length/replicate matrix.

The scope was checked against `source/initial-project-status.txt`, `source/initial-project-diff.txt`, `source/initial-recurrent-transformer-status.txt` and `source/initial-recurrent-transformer-diff.txt` in `.runtime/cdrm-150m-fuzzy-recall/20260908T031418Z`. These records distinguish this milestone from pre-existing uncommitted work.

| Area | Files in this milestone | Concrete change |
| --- | --- | --- |
| Production precision | `recurrent-transformer/olmo/config.py`, `recurrent-transformer/olmo/model.py` | Adds explicit legacy/FP32 ordinary-attention policy, covering pre-attention norm, QKV, Q/K normalization, math SDPA and output projection for every ordinary block. Shared CDRM fabric calls retain their independent precision policy. Unsupported dropout, FlashAttention, nonmatching attention geometry, cache/packing/custom masks, activation checkpointing and parameter/autocast dtypes fail explicitly. |
| Model presets | `configs/cdrm/fuzzy_150m_cdrm.json`, `configs/cdrm/fuzzy_150m_seq.json` | Twelve D1024/H16 blocks, native V16 untied tables, CDRM sites 3/8 and F4096 versus matched SEQ F4192. Fixed rho=1, epsilon=0.1 and lambda=0.01; exact unique parameter counts are checked. |
| Native fuzzy data | `cdrm/mad_data.py`, `tests/test_mad_data.py` | Adds the fuzzy task while preserving the original two-task defaults and arrays. Audits native dense/masked target alignment, padding, variable motifs, repeated keys and partial final values. Separates input-run answer annotation from causal value lookup; retains unknown terminal queries and reports oracle availability and causal shortcuts. The pinned MAD generator is unchanged. |
| Prospective preparation | `scripts/cdrm_fuzzy_prepare.py`, `tests/test_cdrm_fuzzy_prepare.py` | Generates only declared calibration/numerical roles; saves native arrays, source/protocol hashes, overlap and baseline audits, and all 50 epoch permutations. Rejects changed roles/seeds or reused output paths. No final-test role is generated. |
| Paired initialization and calibration | `scripts/cdrm_fuzzy_common.py`, `scripts/cdrm_fuzzy_train.py`, `tests/test_cdrm_fuzzy_train.py` | Introduces a separate fuzzy checkpoint format and guards. Copies 75 equal-shape tensors bitwise; 24 resized MLP tensors retain native initialization, and two CDRM adapters have explicit nonzero draws. The T256 runner supports the three declared LRs, fixed 50-epoch cosine horizon and ten-epoch calibration cap, native metrics, online W&B and exact checkpoint/scheduler/RNG/order continuation. |
| Numerical and operational harnesses | `scripts/cdrm_fuzzy_validate.py`, `scripts/cdrm_fuzzy_profile.py` | Compares actual full-width models from the same saved weights/Adam state under FP32/mixed precision, with optional naive reference, real-loss and raw independent fabric gradients, unscaled states, cotangent scaling, causality and optimizer diagnostics. Retains per-arm raw packets and compiler audits. Profiles the actual update helper after warmup, records peak memory and evaluation cost, and discards operational updates. |
| Precision tests | `tests/test_ordinary_attention_precision.py` | Checks legacy defaults, all 12 precision regions, independent shared-fabric precision and unsupported-input guards; CUDA tests compare SEQ/CDRM × FP32/BF16 with the preserved five-block wrapper, including exact logits, gradients and CDRM states. |
| Aggregation | `scripts/cdrm_fuzzy_report.py`, `tests/test_cdrm_fuzzy_report.py` | Audits supplied-lineage source/data/checkpoint/packet references and saved update order; exports JSON/Markdown/CSV/PNG/SVG and optional W&B summaries. Keeps failed attempts and raw numerical flags, separates initial Adam cosine from trained Adam error, labels timing extrapolations, and requires all six closed `calibration/*-e10` endpoints before LR selection. Prefix/recovery reports remain references; archival staging copies cannot count as new experiments. |
| Selected-checkpoint error analysis | `scripts/cdrm_fuzzy_ceiling_eval.py`, `tests/test_cdrm_fuzzy_ceiling_eval.py` | Replays the authoritative native BF16 development metrics exactly, then evaluates the same saved weights in strict FP32. Reports available/unavailable lookup subsets, eligible-example exact-match denominators and prediction corrections/regressions; saves logits, predictions, masks and error coordinates. Does not change native labels or LR selection and makes no hard accuracy-ceiling claim. |
| Documentation and retained evidence | `docs/cdrm-fuzzy-recall-usage.md`, this report directory, new runtime lineage | Documents task/model/precision/continuation semantics and current qualifications. Runtime-only staging/final-retention helpers preserve closed checkpoints, raw numerical packets, datasets, source/cache archives and failed attempts with checksum verification. Parent numerical artifacts are referenced by their already verified manifest/receipt instead of copied again. Final archival completion requires its receipt. |

The configs' upstream `pad_token_id=0`/`eos_token_id=1` fields are inactive in the audited direct-forward/external-loss path. Native symbols 0/1 remain ordinary keys; padding 15 is handled by the unchanged explicit dense/masked labels. See [reporting usage](reporting-usage.md) for the code evidence. No new attention padding mask or generation behavior is introduced.

## Validation evidence

| Evidence | Observed result | Retained execution log |
| --- | --- | --- |
| Native data and preparation | 58 CPU tests passed, including old-task regression and native edge cases | `execution/fuzzy-data-tests-v1.log` |
| Production precision and existing reference compatibility | 97 CPU tests passed; 19 CUDA tests skipped in that CPU invocation | `execution/precision-final-cpu-regression.log` |
| Targeted production-versus-wrapper checks | Four CUDA tests passed; earlier failed development attempts retained | `execution/precision-cuda-v2.log`, prior `precision-cuda-v1.log` |
| Paired initialization/calibration helper | Seven CPU tests passed | `execution/fuzzy-runner-cpu-tests-final.log` |
| Reporter, including recovery/endpoint distinction | 14 CPU tests passed | `execution/fuzzy-report-tests-v3.log` |
| Selected-checkpoint diagnostic | 13 CPU tests passed; both selected GPU evaluations replay native BF16 metrics exactly and have zero scored BF16→FP32 prediction flips | `analysis/ceiling-eval-source-v2/source-and-tests.json`, `evaluation/selected-{seq,cdrm}-e10-ceiling/report.json`, `analysis/selected-ceiling-review/pair-report.json` |
| Final-retention adapter | Twelve stdlib checks passed, including new evaluation packet references; separate read-only preflight verified 12 native splits and six complete 50-epoch order arrays | `execution/final-retention-cpu-checks-v2.log`, `execution/final-retention-data-preflight-v1.log` |
| Real aggregate | All six endpoints complete; 1,368 referenced files verified, zero integrity/consistency errors, W&B synchronized | `reports/calibration-final/report.json`, `execution/calibration-final-aggregate.log` |

These are individual suites, not an additive unique-test total. The numerical/profile/calibration outcomes are reported separately with exact shapes, state, precision and scope; helper tests do not replace those model checks. The aggregate applies the predeclared native-accuracy selection rule: CDRM LR5e-4 and SEQ LR1e-4. The post-selection diagnostic preserves native error totals (CDRM 16, SEQ 20), including the unavailable targets, and does not revise this decision. Storage completion is established separately by the [verified receipt](storage.json).

## Existing changes outside this milestone

The tiled CDRM core, its earlier FP32/BF16 support, five-block presets and pilot tools, R3 numerical fixes, experimental precision-localization wrappers, shared W&B helper, Docker changes and earlier milestone reports already appear in the initial workspace records. In particular, `olmo/cdrm.py`, `olmo/cdrm_tiled.py` and the earlier CDRM test changes are dependencies rather than new contributions of this milestone. Do not include every currently dirty/untracked file in this PR simply because it is present in the worktree.
