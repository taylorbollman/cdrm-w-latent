# E45 final-evaluation readiness

Read-only review on 2026-09-07, followed by an external stdlib execution helper. No native source/configuration changes, GPU commands, final generation, or final-array inspection occurred. There is no identified code blocker to the planned sequence. At the 15:37 UTC evidence check, SEQ seeds 0/1 and CDRM seed 0 had completed E45/U4500; CDRM seed 1 was still active. The full cohort's successful completion is required before finalization.

The frozen continuation decision selects E45, has `final_access_authorized: false`, and must remain unchanged. Its file SHA-256 is `1e5bfc81ebce25bc8ca974aed102b772f10a20605249ca8d23c354f9b0e8d9a6`; its separate canonical decision digest is `208395fc1d0f0be1f62913b6fdfd758fa88cbb6dc3237086c661495c8203e304`. Both the canonical digest and every recorded input-file hash matched at review. Final split files and the added-final manifest were absent.

After the continuation execution receipt is `complete` with `common_endpoint_verified: true`, use its four `final_members` to create a separate `cdrm-final-endpoint-decision-v1` record. Set `endpoint_epochs: 45`, `endpoint_choice_frozen: true` and `final_access_authorized: true`; retain the selected task/preset/data root and seeds/topologies. Each `cohort` entry maps `member.report.path/sha256` to `report_path/report_sha256`. Do not pass the continuation decision directly to the final planner: its schema and authorization intentionally differ. The complete schema and checkpoint-selection rules are in [the procedure review](final-evaluation-review.md).

Run the planner before adding final data. It supports E45, selects endpoint and earliest minimum development answer CE through E45, and deduplicates shared artifact hashes within each seed/topology. At review, SEQ seed 0's E45 artifact serves both roles; SEQ seed 1's best is E41 and CDRM seed 0's best is E44. Finalize CDRM seed 1's roles from its completed report. The three completed members already imply five unique evaluations, with one or two more for the last member.

Append only the selected copy96 final split using the existing preparation API. The expected final seed is 134567, with 1,280 examples of length 256 and 96 scored copy targets each: 122,880 native and answer targets. The supplemental manifest preserves the original parent/train/dev/permutation files. The final execution helper validates the supplemental parent/config/seed/counts, final manifest and file hashes, and all original plan/continuation input hashes before execution; it rechecks the frozen files before and after each job.

The [execution helper](../../../.runtime/cdrm-naive/20260907T123830Z/execution/run-final-evaluation-plan.py) defaults to validation only:

```text
python3 .runtime/cdrm-naive/20260907T123830Z/execution/run-final-evaluation-plan.py \
  --plan PATH_TO_FINAL_PLAN --continuation-receipt PATH_TO_COMPLETED_CONTINUATION_RECEIPT
```

Add `--execute --receipt PATH_TO_NEW_EXECUTION_RECEIPT` to launch. The helper refuses reused outputs/labels/receipts, serializes the plan's unique jobs through `run-final.py`, and retains success/failure and completed-job records. Its emitted argv prefix exactly matches the launcher's evaluate-only guard; explicit `monitor_every=100` preserves current computational identity. `--run-purpose comparison` permits final evaluation of the compatible SEQ screening lineage. The launcher uses the absolute **16:38:30 UTC final deadline**, separately from the 16:18:30 training cutoff, and refuses starts with under 30 seconds remaining. No deadline extension or CPU fallback is introduced.

Successful final reports must have `SYN-evaluation`, `complete`, `split=final`, the selected checkpoint hash/epoch/update and original identity hash. The helper checks finite CE and count-derived metrics, copy96 native/answer equality, exact example/target denominators, and identical final array identities across all jobs. Reports retain aggregate counts/metrics and identities, not predictions. The subsequent CPU report generator validates checkpoint packets and now includes seed/setting columns in final and pairing CSVs; this report-only metadata correction does not change native identity. The HF wrapper patch remains outside the 40 native source files used for training/evaluation identity.

[Nine stdlib fixture tests](../../../.runtime/cdrm-naive/20260907T123830Z/execution/test-final-evaluation-execution.py) passed. They cover validation without a subprocess, mocked serial role deduplication, incomplete cohorts, changed report/source/final data, wrong seed/config/argv/metrics/checkpoints, reused outputs, deadline exhaustion, and retained failure receipts. Every subprocess in execution tests was mocked. [Raw test log](../../../.runtime/cdrm-naive/20260907T123830Z/execution/test-final-evaluation-execution.log) SHA-256: `2910562c3617471203cfd1cf68a5d1bb26ddbb39bc08894f0138c30c9dcddcd3`. Execution helper SHA-256: `880b1725c7feed47654e2d08cf28be60172a4b989bd9df91320cd276f8ae5852`. This establishes metadata/control-flow checks; actual final execution remains pending.
