Add an opt-in ordinary-autograd FP32 CDRM side memory to the ordinary Transformer backbone. The model produces early and late ordinary previews, reads earlier permanent records plus the current temporary K/V pair, writes the updated record afterward, and bridges the proposed state into the ordinary suffix. The side scan shares the early block's canonical fused-QKV, normalization and MLP parameters through differentiable slices and registers only two nonzero bias-free adapters.

The reference supports configurable early/late sites, active same-depth and current-only controls, and optional graph-connected diagnostics. It rejects unsupported precision/cache/packing configurations. CDRM stays disabled by default; the HF wrapper preserves its prior tuple layout when native output includes the new optional field. The selected six-block profile uses D128/H16/MLP512, sites 1/3 and gates epsilon 0.1, rho 1, lambda 0.01.

The accompanying MAD workflow retains native aligned supervision, fixed audited data, matched fresh backbone initialization and ordered epoch batches, explicit checkpoint roles, and exact recovery checks. It screens SEQ6 before allocating CDRM runs and produces CPU-only JSON/CSV/PNG/SVG reports from completed records. Native dense recall training CE is reported separately from answer-only evaluation CE.

Validation is complete for the declared reference scope:

- 60 focused CPU reference tests, including independent FP64 all-gradient oracles, shared-owner gradient sums, earlier deep-state credit, read-before-write, causality, isolation, bypass and exact next-Adam-update reload.
- 60 existing foundation/conversion CPU tests passed; 20 CUDA-only tests skipped. Four HF tuple compatibility tests passed. The MAD data suite passed 29 CPU tests; 18 separate standard-library checks cover screening/retention logic.
- Selected copying B128 GPU NUM passes exact lambda-zero logits/all 51 common gradients, causal isolation, finite FP32 active gradients/moments and model/optimizer/logit round trip. Six-block copying update 4→8 recovery matches uninterrupted state and non-timing metrics exactly. A repeated 16-example recall OPS run reaches 100% training token/exact accuracy; this is a fitting diagnostic.

The current research setting is official MAD copying V16/T256/K96. Both SEQ6 seeds completed 25 epochs, reaching 48.289% and 66.184% dev token accuracy, with zero whole-example exact matches. The development-only selection and B128 cost decision were frozen before CDRM dev access. The initial SEQ12 recall run remains historical calibration; no CDRM12 SYN arm launched. This is an adapted local experiment, not a reproduction of the paper's tuned depth/hyperparameter sweep.

The naive CDRM reference costs 0.862111 seconds per measured selected-shape update versus SEQ6's 0.045803 seconds: 18.82× slower and 1.75× peak allocated memory, for 32,768 extra parameters. These synchronized loss/backward/Adam measurements exclude evaluation/startup/checkpointing and do not establish equal-time learning efficiency.

Both epoch-25 seed pairs pass initial-tensor, adapter-inventory, data/source/update and checkpoint audits. Seed 0 dev token accuracy is 48.289% SEQ versus 40.645% CDRM, with CE 0.883124 versus 1.144615. Seed 1 is 66.184% versus 63.584%, with CE 0.746556 versus 0.725343. Every arm has zero exact matches. These remain intermediate development observations.

The immutable cost-only decision at 14:53:44 UTC selected common epoch **45**: its 4,462.77-second estimate, including 15% margin, fits the 5,085.70-second remaining training window, while epoch 50's 5,561.22-second estimate does not. The original 16:18:30 UTC cutoff and finalization reserve remain fixed. Seed 0 has now completed and passed the epoch-45 lineage/checkpoint audit: SEQ CE0.398816/token81.417%, CDRM CE0.439443/token76.562%, both exact0. CDRM best-dev is epoch44 with CE0.416740/token81.865%; SEQ best-dev is epoch45. These role comparisons use different epochs. `PENDING_COMMON45_COMPLETION`: CDRM seed 1 is still active; its last paired result is E25, which is not pooled with seed0E45. `PENDING_FINAL_EVALUATION`: explicit final data/evaluation follows the frozen endpoint/checkpoint-selection rules. `PENDING_FINAL_RETENTION`: completed-run archival is separate from closing the active comparisons and final reports. See [the results draft](results.md) and [frozen decision](six-continuation-decision.json). No final-holdout or broad CDRM learning advantage is claimed.
