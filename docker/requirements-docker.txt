# Add project-specific Python packages here only when they are not already
# supplied by nvcr.io/nvidia/pytorch:26.06-py3.
#
# Be conservative with CUDA/PyTorch-adjacent packages. Prefer the versions
# shipped by the NGC image unless a project script proves we need a different
# build.

# FlashAttention-4 is currently beta/pre-release only. Pin the exact beta
# because pip will not select it from an unpinned requirement by default.
flash-attn-4[cu13]==4.0.0b20

# Hugging Face training stack.
transformers==5.13.0
huggingface-hub==1.22.0
datasets==5.0.0
google-cloud-storage==3.12.0
lightning==2.6.0
deepspeed==0.19.0
sentencepiece==0.2.1

# Repository test runner.
pytest==9.1.1

# Recurrent OLMo dependencies resolved against the existing Python 3.12 NGC stack.
# numpy<2 is required by the upstream package; preserve the NGC Torch/CUDA stack.
ai2-olmo-core==2.6.0
bettermap==1.3.1
boto3==1.43.89
botocore==1.43.89
cached_path==1.8.10
dataclass-extensions==0.5.0
importlib_resources==7.1.0
jmespath==1.1.0
numpy==1.26.4
rich==13.9.4
s3transfer==0.19.2
